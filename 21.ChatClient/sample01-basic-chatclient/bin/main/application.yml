spring:
  application:
    name: chatclient-basic-sample
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4o-mini
          temperature: 0.7

server:
  port: 9100

logging:
  level:
    org.springframework.ai: DEBUG
