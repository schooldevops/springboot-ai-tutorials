# 프로젝트 생성 가이드

이 가이드는 Spring Initializr를 사용하여 Spring AI 프로젝트를 생성하는 상세한 방법을 안내합니다.

## 🌐 방법 1: 웹 브라우저 사용

### 단계별 절차

#### 1단계: Spring Initializr 접속

브라우저에서 https://start.spring.io/ 접속

#### 2단계: 프로젝트 설정

**Project (프로젝트 타입):**
- ✅ Gradle Project (권장)
- 또는 Maven Project

**Language (언어):**
- ✅ Kotlin

**Spring Boot (버전):**
- ✅ 3.3.0 (또는 최신 버전)

**Project Metadata (프로젝트 메타데이터):**
```
Group: com.example
Artifact: spring-ai-app
Name: spring-ai-app
Description: Spring AI with Kotlin Application
Package name: com.example.springai
Packaging: Jar
Java: 17 (또는 21)
```

#### 3단계: 의존성 추가

"Dependencies" 섹션에서 "Add Dependencies" 버튼 클릭 후 다음 검색 및 추가:

**필수 의존성:**
1. **Spring Web**
   - 검색: "web"
   - 선택: "Spring Web"

2. **Spring AI OpenAI**
   - 검색: "spring ai openai"
   - 선택: "Spring AI OpenAI"
   - 또는 **Spring AI Anthropic** (Claude 사용 시)
   - 또는 **Spring AI Ollama** (로컬 LLM 사용 시)

#### 4단계: 프로젝트 생성

1. "Generate" 버튼 클릭
2. ZIP 파일 자동 다운로드

#### 5단계: 프로젝트 열기

```bash
# 압축 해제
unzip spring-ai-app.zip

# 디렉토리 이동
cd spring-ai-app

# IntelliJ IDEA에서 열기
idea .  # 또는 IntelliJ에서 File → Open
```

---

## 💻 방법 2: IntelliJ IDEA 사용

### 단계별 절차

#### 1단계: 새 프로젝트 시작

1. IntelliJ IDEA 실행
2. `File` → `New` → `Project`

#### 2단계: Spring Initializr 선택

- Name: `spring-ai-app`
- Location: 원하는 디렉토리 선택
- Language: **Kotlin**
- Type: **Gradle** (권장) 또는 Maven
- JDK: 17 이상 선택
- Spring Boot: 최신 버전 선택

#### 3단계: 의존성 선택

다음 의존성을 검색하여 추가:

- `Spring Web`
- `Spring AI OpenAI` (또는 다른 LLM 통합)

#### 4단계: 프로젝트 생성

- Finish 버튼 클릭
- IntelliJ가 자동으로 프로젝트 구성

---

## ⌨️ 방법 3: CLI 사용

### cURL 명령어

```bash
curl https://start.spring.io/starter.zip \
  -d type=gradle-project-kotlin \
  -d language=kotlin \
  -d bootVersion=3.3.0 \
  -d baseDir=spring-ai-app \
  -d groupId=com.example \
  -d artifactId=spring-ai-app \
  -d name=spring-ai-app \
  -d description="Spring AI with Kotlin" \
  -d packageName=com.example.springai \
  -d packaging=jar \
  -d javaVersion=17 \
  -d dependencies=web,spring-ai-openai-starter \
  -o spring-ai-app.zip

# 압축 해제
unzip spring-ai-app.zip

# 디렉토리 이동
cd spring-ai-app
```

### HTTPie 사용 (대안)

```bash
http https://start.spring.io/starter.zip \
  type==gradle-project-kotlin \
  language==kotlin \
  bootVersion==3.3.0 \
  baseDir==spring-ai-app \
  groupId==com.example \
  artifactId==spring-ai-app \
  dependencies==web,spring-ai-openai-starter \
  -o spring-ai-app.zip
```

---

## 📋 생성된 파일 확인

프로젝트 생성 후 다음 구조가 있어야 합니다:

```
spring-ai-app/
├── build.gradle.kts          # Gradle 빌드 파일
├── settings.gradle.kts       # Gradle 설정 파일
├── gradle/                   # Gradle Wrapper
│   └── wrapper/
├── src/
│   ├── main/
│   │   ├── kotlin/.../SpringAiAppApplication.kt
│   │   └── resources/application.properties
│   └── test/
│       └── kotlin/.../SpringAiAppApplicationTests.kt
└── README.md
```

---

## ⚙️ 프로젝트 설정

### 1. Gradle Wrapper 확인

프로젝트에 `gradlew` 파일이 있는지 확인:

```bash
ls -la | grep gradlew
```

없다면 생성:

```bash
gradle wrapper --gradle-version 8.5
```

### 2. 환경 변수 설정

API Key를 환경 변수로 설정:

**macOS / Linux:**
```bash
export OPENAI_API_KEY="sk-your-api-key-here"
```

**Windows (PowerShell):**
```powershell
$env:OPENAI_API_KEY="sk-your-api-key-here"
```

### 3. application.yml 설정

`src/main/resources/application.yml` 파일 생성 또는 수정:

```yaml
spring:
  application:
    name: spring-ai-app
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4
          temperature: 0.7

server:
  port: 8080
```

---

## ✅ 검증

프로젝트가 올바르게 생성되었는지 확인:

```bash
# 빌드 테스트
./gradlew build

# 실행 테스트
./gradlew bootRun
```

애플리케이션이 정상적으로 시작되면 성공!

---

## 🔄 기존 프로젝트에 Spring AI 추가

이미 Spring Boot 프로젝트가 있다면:

### 1. build.gradle.kts에 의존성 추가

```kotlin
dependencies {
    implementation("org.springframework.ai:spring-ai-openai-spring-boot-starter:1.0.0")
}
```

### 2. application.yml 설정 추가

```yaml
spring:
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
```

### 3. 프로젝트 동기화

```bash
./gradlew build
```

---

## 📚 참고 자료

- [Spring Initializr 공식 사이트](https://start.spring.io/)
- [Spring AI 공식 문서](https://docs.spring.io/spring-ai/reference/)
- [Gradle 공식 문서](https://docs.gradle.org/)

---

## ❓ 자주 묻는 질문

### Q: Maven을 사용해도 되나요?

A: 네, 가능합니다. 하지만 Kotlin 프로젝트에서는 Gradle (Kotlin DSL)을 권장합니다.

### Q: Spring AI 버전은 어떻게 확인하나요?

A: [Maven Central](https://mvnrepository.com/artifact/org.springframework.ai)에서 최신 버전을 확인하세요.

### Q: 여러 LLM을 동시에 사용할 수 있나요?

A: 네, 가능합니다. 각각 다른 Bean으로 등록하고 `@Primary`로 기본값을 지정하세요.

---

이제 프로젝트가 준비되었습니다! 다음 단계로 진행하세요. 🚀

