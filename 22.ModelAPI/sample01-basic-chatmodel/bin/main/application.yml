spring:
  application:
    name: chatmodel-basic-sample
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4o-mini
          temperature: 0.7
server:
  port: 9200
logging:
  level:
    org.springframework.ai: DEBUG
