spring:
  application:
    name: simple-chatbot
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:your-api-key-here}
      chat:
        options:
          model: gpt-4o-mini
          temperature: 0.7

server:
  port: 9000

logging:
  level:
    org.springframework.ai: DEBUG
